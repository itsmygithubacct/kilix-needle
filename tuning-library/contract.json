{
 "confirmation": {
  "always": [
   "close_pane",
   "close_tab",
   "run_in_pane"
  ],
  "partial_refusal": "requires human confirmation even with --yes",
  "with_program": [
   "open_pane",
   "open_tab"
  ]
 },
 "constraints": {
  "commands": "exact case-sensitive request span; quotes are wrappers",
  "move_tab": "exactly one of direction and position",
  "pronouns": "close it is refused; explicit targets required",
  "target": "current is the caller; named targets normalize to name:<value>"
 },
 "defaults": {
  "maximize_pane": {
   "pane": "current",
   "restore": false
  },
  "rename_pane": {
   "pane": "current"
  },
  "resize_pane": {
   "amount": 2
  }
 },
 "implementation_sha256": {
  "actions.py": "3980c4bcf9d9e1429f57400adb75ee88b6a3e6cf572d6113b4dcec849f890cf4",
  "domain_bridge.py": "122264046715d402f9fb3aa592f95f6ad6d79b18a317aa3bc86d558956bfd189",
  "kilix.py": "72d04765b84ab65b1cf5d4dbc3eaf49d05c6f48f9ac6121449c5a71e03d8cb6d",
  "needle_cli.py": "4be595466595af619a84bc4de8464282a5462d80f3e984fd27c2f61a62e6d80b"
 },
 "limits": {
  "calls": 32,
  "request_bytes": 4096
 },
 "sha256": "f46cfee27184c48db442b89f6c9b431a4459c847e1c37dfffda2c84814b68d0f",
 "tools": [
  {
   "description": "Split the window: open a new pane next to the current one. Use for split, new pane, open X in a pane",
   "name": "open_pane",
   "parameters": {
    "additionalProperties": false,
    "properties": {
     "name": {
      "description": "what to call the pane, only if the user names it",
      "type": "string",
      "x-source": "input"
     },
     "program": {
      "description": "program to start in it, only if the user names one",
      "type": "string",
      "x-source": "input"
     },
     "side": {
      "description": "which side of the current pane",
      "enum": [
       "left",
       "right",
       "above",
       "below"
      ],
      "type": "string"
     }
    },
    "required": [],
    "type": "object"
   }
  },
  {
   "description": "Open a new tab. Use for new tab, open X in a tab",
   "name": "open_tab",
   "parameters": {
    "additionalProperties": false,
    "properties": {
     "name": {
      "description": "what to call the tab, only if the user names it",
      "type": "string",
      "x-source": "input"
     },
     "program": {
      "description": "program to start in it, only if the user names one",
      "type": "string",
      "x-source": "input"
     }
    },
    "required": [],
    "type": "object"
   }
  },
  {
   "description": "Close, kill or quit a pane. Only when the user says close, kill, quit or exit",
   "name": "close_pane",
   "parameters": {
    "additionalProperties": false,
    "properties": {
     "pane": {
      "description": "current, left, right, above, below, or the name or program of a pane",
      "type": "string",
      "x-source": "input-or-canonical"
     }
    },
    "required": [
     "pane"
    ],
    "type": "object"
   }
  },
  {
   "description": "Close, kill or quit a tab. Only when the user says close, kill, quit or exit",
   "name": "close_tab",
   "parameters": {
    "additionalProperties": false,
    "properties": {
     "tab": {
      "description": "current, a tab number, or the name or program of a tab",
      "type": "string",
      "x-source": "input-or-canonical"
     }
    },
    "required": [
     "tab"
    ],
    "type": "object"
   }
  },
  {
   "description": "Go to, switch to, move to or focus another pane without changing it",
   "name": "go_to_pane",
   "parameters": {
    "additionalProperties": false,
    "properties": {
     "pane": {
      "description": "left, right, above, below, or the name or program of a pane",
      "type": "string",
      "x-source": "input-or-canonical"
     }
    },
    "required": [
     "pane"
    ],
    "type": "object"
   }
  },
  {
   "description": "Go to or switch to another tab without changing it",
   "name": "go_to_tab",
   "parameters": {
    "additionalProperties": false,
    "properties": {
     "tab": {
      "description": "next, previous, a tab number, or the name or program of a tab",
      "type": "string",
      "x-source": "input-or-canonical"
     }
    },
    "required": [
     "tab"
    ],
    "type": "object"
   }
  },
  {
   "description": "Rearrange the panes in this tab",
   "name": "arrange_panes",
   "parameters": {
    "additionalProperties": false,
    "properties": {
     "layout": {
      "description": "grid; stack shows one pane at a time; vertical is side by side; horizontal is one above another",
      "enum": [
       "tall",
       "fat",
       "grid",
       "horizontal",
       "vertical",
       "splits",
       "stack"
      ],
      "type": "string"
     }
    },
    "required": [
     "layout"
    ],
    "type": "object"
   }
  },
  {
   "description": "Rename or call the current tab something",
   "name": "rename_tab",
   "parameters": {
    "additionalProperties": false,
    "properties": {
     "name": {
      "description": "the new name",
      "type": "string",
      "x-source": "input"
     }
    },
    "required": [
     "name"
    ],
    "type": "object"
   }
  },
  {
   "description": "Make the current pane wider, narrower, taller or shorter",
   "name": "resize_pane",
   "parameters": {
    "additionalProperties": false,
    "properties": {
     "amount": {
      "description": "number of cells, only if the user gives one",
      "maximum": 50,
      "minimum": 1,
      "type": "integer"
     },
     "direction": {
      "enum": [
       "wider",
       "narrower",
       "taller",
       "shorter"
      ],
      "type": "string"
     }
    },
    "required": [
     "direction"
    ],
    "type": "object"
   }
  },
  {
   "description": "Type a command into an existing pane and press enter. Use for run X in a pane, type X",
   "name": "run_in_pane",
   "parameters": {
    "additionalProperties": false,
    "properties": {
     "command": {
      "description": "the exact command text",
      "type": "string",
      "x-source": "input"
     },
     "pane": {
      "description": "current, left, right, above, below, or the name or program of a pane",
      "type": "string",
      "x-source": "input-or-canonical"
     }
    },
    "required": [
     "pane",
     "command"
    ],
    "type": "object"
   }
  },
  {
   "description": "Show one pane full-size (zoom or maximize it), or restore the previous layout",
   "name": "maximize_pane",
   "parameters": {
    "additionalProperties": false,
    "properties": {
     "pane": {
      "description": "current, left, right, above, below, or the name or program of a pane",
      "type": "string",
      "x-source": "input-or-canonical"
     },
     "restore": {
      "description": "true to un-maximize and restore the layout",
      "type": "boolean"
     }
    },
    "required": [],
    "type": "object"
   }
  },
  {
   "description": "Rename or title a pane",
   "name": "rename_pane",
   "parameters": {
    "additionalProperties": false,
    "properties": {
     "name": {
      "description": "the new name",
      "type": "string",
      "x-source": "input"
     },
     "pane": {
      "description": "current, left, right, above, below, or the name or program of a pane",
      "type": "string",
      "x-source": "input-or-canonical"
     }
    },
    "required": [
     "name"
    ],
    "type": "object"
   }
  },
  {
   "description": "Swap the current pane with its neighbour on one side",
   "name": "swap_panes",
   "parameters": {
    "additionalProperties": false,
    "properties": {
     "side": {
      "description": "which neighbour to swap with",
      "enum": [
       "left",
       "right",
       "above",
       "below"
      ],
      "type": "string"
     }
    },
    "required": [
     "side"
    ],
    "type": "object"
   }
  },
  {
   "description": "Move the current tab left or right in the tab bar, or to a position",
   "name": "move_tab",
   "parameters": {
    "additionalProperties": false,
    "properties": {
     "direction": {
      "enum": [
       "left",
       "right"
      ],
      "type": "string"
     },
     "position": {
      "description": "tab position, only if the user gives one",
      "maximum": 9,
      "minimum": 1,
      "type": "integer"
     }
    },
    "required": [],
    "type": "object"
   }
  }
 ],
 "version": "kilix-panes-tools/v2"
}
